# Stripe API 연결 가이드 (단계별)

## 📋 1단계: Stripe 계정 만들기

1. **Stripe 웹사이트 접속**
   - https://dashboard.stripe.com/register
   - 이메일로 가입 (무료)

2. **대시보드 접속**
   - 가입 후 https://dashboard.stripe.com 접속
   - 로그인

## 🔑 2단계: API 키 발급받기

1. **API Keys 페이지로 이동**
   - 왼쪽 메뉴에서 "Developers" 클릭
   - "API keys" 클릭
   - 또는 직접: https://dashboard.stripe.com/apikeys

2. **테스트 모드 키 복사**
   - 페이지 상단에 "Test mode" 토글이 켜져 있는지 확인
   - **Publishable key** 복사 (pk_test_로 시작)
   - **Secret key** 옆의 "Reveal test key" 클릭 후 복사 (sk_test_로 시작)

## 💻 3단계: 로컬 환경 변수 설정

### 방법 1: 터미널에서 생성

```bash
# 프로젝트 폴더로 이동
cd /Users/dobby/my-store

# .env.local 파일 생성
cat > .env.local << 'EOF'
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_여기에_실제_키_입력
STRIPE_SECRET_KEY=sk_test_여기에_실제_키_입력
EOF
```

### 방법 2: 직접 파일 생성

1. 프로젝트 루트 폴더(`/Users/dobby/my-store`)에 `.env.local` 파일 생성
2. 다음 내용 입력 (실제 키로 교체):

```env
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_51QZxXZP3xYzAbCdEfGhIjKlMnOpQrStUvWxYz
STRIPE_SECRET_KEY=sk_test_51QZxXZP3xYzAbCdEfGhIjKlMnOpQrStUvWxYz
```

**중요**: 
- `pk_test_...` 부분을 실제 Publishable key로 교체
- `sk_test_...` 부분을 실제 Secret key로 교체
- 따옴표 없이 입력

## 🚀 4단계: 개발 서버 재시작

환경 변수를 변경한 후에는 서버를 재시작해야 합니다:

```bash
# 개발 서버가 실행 중이면 Ctrl+C로 중지 후
npm run dev
```

## 🌐 5단계: Vercel 배포 환경 설정

배포된 사이트에서도 결제가 작동하도록 Vercel에 환경 변수를 설정해야 합니다:

1. **Vercel 대시보드 접속**
   - https://vercel.com/dashboard
   - 로그인

2. **프로젝트 선택**
   - `my-store` 프로젝트 클릭

3. **환경 변수 설정**
   - 상단 메뉴에서 "Settings" 클릭
   - 왼쪽 메뉴에서 "Environment Variables" 클릭
   - "Add New" 버튼 클릭

4. **변수 추가 (2개)**
   
   **첫 번째 변수:**
   - Key: `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`
   - Value: Stripe에서 복사한 Publishable key (pk_test_...)
   - Environment: Production, Preview, Development 모두 선택
   - "Save" 클릭

   **두 번째 변수:**
   - Key: `STRIPE_SECRET_KEY`
   - Value: Stripe에서 복사한 Secret key (sk_test_...)
   - Environment: Production, Preview, Development 모두 선택
   - "Save" 클릭

5. **재배포**
   - "Deployments" 탭으로 이동
   - 최신 배포의 "..." 메뉴 클릭
   - "Redeploy" 선택
   - 또는 자동으로 재배포될 때까지 대기

## ✅ 6단계: 테스트

### 로컬 테스트
1. http://localhost:3000 접속
2. 제품의 "구매하기" 버튼 클릭
3. 결제 정보 입력
4. "결제하기" 클릭
5. Stripe Checkout 페이지에서 테스트 카드 입력:
   - 카드 번호: `4242 4242 4242 4242`
   - 만료일: `12/25` (미래 날짜)
   - CVC: `123`
   - 우편번호: `12345`

### 배포 사이트 테스트
1. 배포된 사이트 URL 접속
2. 동일한 테스트 카드로 결제 테스트

## 🔍 문제 해결

### "STRIPE_SECRET_KEY is not set" 에러가 나는 경우
- `.env.local` 파일이 프로젝트 루트에 있는지 확인
- 파일 이름이 정확히 `.env.local`인지 확인 (앞에 점 포함)
- 서버를 재시작했는지 확인
- 환경 변수 값에 따옴표나 공백이 없는지 확인

### Vercel에서 결제가 안 되는 경우
- Vercel 대시보드에서 환경 변수가 제대로 설정되었는지 확인
- 재배포를 했는지 확인
- 브라우저 콘솔(F12)에서 에러 메시지 확인

## 📞 추가 도움말

- Stripe 공식 문서: https://stripe.com/docs
- Stripe 테스트 카드: https://stripe.com/docs/testing
